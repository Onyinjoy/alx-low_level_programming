#include "main.h"
/**
 * add - function that adds numbers
 * @a: parameters to be checked
 * @b: parameter to be checked
 * Return: always i
 */
int add(int a, int b)
{
	int i;

	i = (a + b);
	return (i);
}
